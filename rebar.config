%% Set dir where external (downloaded) applications are put
{deps_dir, ["deps"]}.

%% External dependencies
{deps, [
        %% Logging library from basho
        {lhttpc, ".*",
        {git, "git://github.com/oscarh/lhttpc", {branch, "master"}}}
]}.

